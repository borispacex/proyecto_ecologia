import { SearchProyectosPipe } from './search-proyectos.pipe';

describe('SearchProyectosPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchProyectosPipe();
    expect(pipe).toBeTruthy();
  });
});
