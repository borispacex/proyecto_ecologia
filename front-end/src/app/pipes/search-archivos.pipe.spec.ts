import { SearchArchivosPipe } from './search-archivos.pipe';

describe('SearchArchivosPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchArchivosPipe();
    expect(pipe).toBeTruthy();
  });
});
