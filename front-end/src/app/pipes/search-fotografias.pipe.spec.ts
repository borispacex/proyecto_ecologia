import { SearchFotografiasPipe } from './search-fotografias.pipe';

describe('SearchFotografiasPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFotografiasPipe();
    expect(pipe).toBeTruthy();
  });
});
