import { SearchInvestigadoresPipe } from './search-investigadores.pipe';

describe('SearchInvestigadoresPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchInvestigadoresPipe();
    expect(pipe).toBeTruthy();
  });
});
