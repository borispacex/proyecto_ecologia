import { SearchAdmUsuarioRolPipe } from './search-adm-usuario-rol.pipe';

describe('SearchAdmUsuarioRolPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAdmUsuarioRolPipe();
    expect(pipe).toBeTruthy();
  });
});
