import { SearchDifusionesPipe } from './search-difusiones.pipe';

describe('SearchDifusionesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDifusionesPipe();
    expect(pipe).toBeTruthy();
  });
});
